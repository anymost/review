/**
 * 一 type:
 *          string number boolean Array<> any void null undefined never
 *          类型断言 (<string>name) (name as string)
 * 二 接口:
 *           可选属性? 只读属性readonly 只读数组 ReadonlyArray<number>
 * 三 函数
 *          定义函数的形式 使用接口描述函数  使用接口调用签名
 *          可索引类型
 */


namespace Person{
    export let age = 1;
}