/**
 * Created by lenovo on 2017/6/15.
 */
/**
 * store
 * reducer
 * actionCreator
 * store.getState()
 * store.dispatch()
 * store.subscribe()
 *
 *
 *  redux的流程
 *  在事件处理函数中，调用store.dispatch()
 *  然后在reducer中会返回新的state
 *  state改变会自动调用store.subscribe()来渲染视图
 *  最后通过store.getState()拿到状态
 *
 *
 *
 *
 *
 */